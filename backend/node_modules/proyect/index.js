#!/usr/bin/env node

let parse = require("./src/parse"),
    context = require("./src/context");

function run() {
    let { run, params, local } = parse(process.argv.slice(2));
    require(local ? `./${run}` : run)(context(params));
}

run();
